import { scientistAttr } from 'sortable-table/helpers/scientist-attr';
import { module, test } from 'qunit';

module('Unit | Helper | scientist attr');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scientistAttr([42]);
  assert.ok(result);
});
